services:
  contenttracker:
    build: .
    expose:
      - "5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - coolify
    restart: unless-stopped

networks:
  coolify:
    external: true
